import 'package:flutter/material.dart';

class AutomationTab extends StatelessWidget {
  const AutomationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Automation'));
  }
}
